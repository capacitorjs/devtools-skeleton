'use strict';

import {ContentHelpers} from 'capacitor-devtools-helpers';

const portName = 'github.com/capacitorjs/devtools-skeleton:content';
ContentHelpers.initializeContent(portName).then(function () {
  // start
});